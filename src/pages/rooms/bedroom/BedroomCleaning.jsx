import ProcessPage from '../../../components/ProcessPage';
import { processPages } from '../../../content/siteContent';

const BedroomCleaning = () => {
  return <ProcessPage page={processPages.bedroom} />;
};

export default BedroomCleaning;
